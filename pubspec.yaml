name: flutter_widgetkit_sample
description: iOS WidgetKit sample

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  # Riverpod
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^1.0.0
  freezed_annotation: ^1.1.0
  json_annotation: ^4.4.0
  home_widget: ^0.1.5

  shared_preferences: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:
  uses-material-design: true